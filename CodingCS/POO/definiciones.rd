Herencia Polimorfismo Encapsulamiento 

        convenciones 
    los public deben comenzar con mayusc ej. HolaMundo
    los no public deben empezar con minusc ej. holaMundo


        CONSTRUCTORES 
    si queremos tener diferentes estados iniciales en un contructor debemos crear mas, a eso se le llama 
    sobrecarga de constructores cuando la clase tiene mas de un contructor

    cada constructor debe recibir diferente numeros de parametros

    no hay limite solo debemos dar diferentes numeros de parametros  